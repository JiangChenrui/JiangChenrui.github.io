// Custom styles.
body{
    background:url(/images/back1.jpg);
    background-repeat: no-repeat; // 如果背景图片不能全屏，那么是否平铺显示，充满屏幕
    background-attachment: fixed; // 背景是否随着网页上下滚动而滚动，fixed 为固定
    background-size: cover;       //填充
    background-position:100% 50%;
}

main-inner { 
    margin-top: 60px;
    padding: 60px 60px 60px 60px;
    background: #fff;
    min-height: 500px;
}
.post-toc .nav .nav-child { 
    display: block;
}
.sidebar {
    opacity: 0.9;
    box-shadow: none
    background: none
}
// 透明度设置
.content-wrap {
  opacity: 0.9;
}

.header-inner {
  background: rgba(255,255,255,0.9);
}

.popup {
  opacity: 0.9;
}
